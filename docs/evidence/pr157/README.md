# PR 157 Evidence

This screenshot was captured from a viewer generated from a real trace under
`.traces/`. Nested request and response payloads were collapsed before capture
so the evidence shows the JSON tree interaction without exposing headers or
prompt content.

