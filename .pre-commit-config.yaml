repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.11.12
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

  - repo: local
    hooks:
      - id: pytest
        name: pytest
        entry: uv run pytest -x -q --timeout=60
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]

      - id: legibility
        name: legibility
        entry: uv run python scripts/check_legibility.py
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
